# Work log

- Established the initial Earth Through Time shell: a compact timeline header and a CSS-only deep-space background. Kept the stage empty so later scroll and globe work has a clean foundation.
- Added the opening hero with a centred title and a single-sentence introduction that explains both the story's span and the scroll mechanic without competing with the future globe.
- Built the core scroll-as-time experience around one sticky, rotating 3D Earth. Twelve weighted eras now crossfade through procedural surface textures while a shared typed timeline updates the date and short explanation; the present is intentionally compressed near the end, and the pure mapping is unit-tested.
- Kept a CSS globe and chronological era list as the no-WebGL/no-JavaScript fallback, so the same story remains readable rather than collapsing to an empty stage.
- Expanded the narrative from 12 to 42 events after the initial version felt too sparse beside a full formation-to-destruction timeline. Added geological boundaries, all five major mass-extinction intervals, evolutionary transitions, human-scale events and more future states while retaining the non-linear scale.
- Switched procedural globe textures to a six-item on-demand cache. The denser event list now adds narrative depth without allocating a separate 1024-pixel planet texture for every stop at startup.
- Rebalanced the chronology after the past dominated 93% of the scroll. “Now” sits at 58%, leaving 42% for 27 researched future events (62 total): conditional glaciation, tectonic scenarios, a brighter Sun, eclipse loss, atmospheric deoxygenation, revised vegetation limits, ocean and microbial decline, and distinct late-Sun phases through the white-dwarf era. Model-dependent claims are labelled as such, including the newly uncertain fate of Earth during the Sun’s giant phases.
- Hardened the agent harness so `spec/invariants.test.ts` remains immutable and additional tests require an explicit user request; removed the obsolete starter example as directed by `spec/README.md`.
- Trimmed `CLAUDE.md` to the active work, dependency-safety, and process-logging guidance, removing the opening course overview and everything after the process-logging section.
- Replaced the visibly tiled CSS star pattern with a deterministic canvas field whose positions, sizes, brightness and colour temperatures vary naturally without repeating. Removed the era-coloured background bloom and every dedicated globe halo/rim layer so the planet now has a clean silhouette.
- Increased the starfield to roughly four times its prior population, separating it into a faint deep field and brighter foreground. Expanded the temperature palette, size hierarchy, rare diffraction details and occasional close pairs to add variety without restoring an artificial pattern.
- Rebuilt the planet as a layered renderer: higher-resolution textured terrain, an independent ocean/specular mask, a separately rotating cloud shell, and a true atmospheric-scattering shell now respond to the same era transitions without flattening into one painted sphere.
- Reworked the lighting and colour pipeline with ACES tone mapping, stronger daylight, restrained ocean glints, terrain relief, biome variation and a brighter atmospheric limb. The no-WebGL globe received matching depth, cloud and atmosphere treatments so its fallback no longer reads as a flat disc.
- Added local day, ocean, cloud and night-light maps from the official Three.js example texture collection for the present-day Earth, while preserving generated era-specific geography for deep history and the far future.
- Reworked the late-stage Sun from a translucent blurred wash into an opaque stellar disc with a white-gold photosphere, darker limb, subtle surface variation and a broader coloured corona. Its existing timeline strength and size transitions remain intact.
- Split the final white dwarf from the red-giant treatment. The old warm, spotted miniature disc now crossfades into a compact blue-white photosphere with a sharp limb, cool halo and fine diffraction rays as the giant sheds its envelope.
- Made the present moment physically linger for a little over one viewport of native scrolling, with a filling hold indicator so the resistance feels intentional rather than broken. Added a present-only reflection that compares written history to the final tenth of a 24-hour Earth and frames humanity as both vanishingly brief and capable of choosing what follows; the mobile present composition gives the reflection extra stage space so it stays readable at the marked viewport.
- Added a cratered Moon that forms during the Moon-forming impact, remains beside Earth through the timeline, and shares Earth’s interpolated late-life heating so it turns red and develops a matching glow as the Sun expands.
- Corrected the white-dwarf ending so the Moon fades out through Earth’s uncertain-fate transition and is absent once the Sun is dead, matching the deliberately unresolved final state.
- Reserved 1.1 viewport-heights of pinned scrolling after the white-dwarf state reaches completion, giving the final copy time to be read before the timeline releases into the conclusion.
- Retargeted the conclusion's return link from the vertically centred title to the top-level page landmark, so activating it returns to the document's true beginning.
- Added a dependency-free performance regression suite that budgets the built payload, compressed client bundle, request graph and asset sizes, and benchmarks the scroll-to-era mapping hot path. Added `pnpm test:performance` and documented that the checks measure `dist/`.
- Added correctness coverage for timeline data and interpolation, the built chronological fallback, and interactive scroll-driven DOM/accessibility updates. Made `pnpm test` build before testing so built-page checks are reliable, and documented focused and full test commands in `CLAUDE.md`.
- Added the new correctness suite to `PROCESS.md` as a harness-level feedback improvement, including why building before built-output tests matters and the passing 35-test result.
- Reverted the scroll-resistance work from merged PRs #9 and #10 and closed unmerged PR #11 after real use exposed halted scrolling, jumping copy, and jarring transitions; restored the simpler pre-PR-9 timeline and documented why interaction feedback outweighed green logic tests.
- Fixed a visual-state inheritance bug found during direct use: the +10, +50, +500 and +600 million-year entries borrowed the present preset and therefore reloaded today's photographic map and city lights. Added a separate temperate procedural mode and a regression test reserving the photographic mode for the actual present.
- Corrected the red-giant disc chronology: early future eras were ramping `visual.sun` from ~+500 Myr, so the swollen Sun appeared billions of years before it leaves the main sequence. Kept planet heating via `heat`, held the disc near zero until `core-hydrogen-ends`, and stopped treating the sterile dry-cinder / late main-sequence stops as red-giant mode.
- Decoupled the Moon’s orange glow from Earth’s climate `heat`. Future Moon heating now tracks the expanding Sun (scaled onto the CSS glow threshold), so the Moon stays grey through ocean loss and only reddens once solar expansion begins; early impact/molten eras still use heat.
- Strengthened future Moon glow mapping so +5.4 BY (core hydrogen ends) is a clear visible start (~0.75 heat / ~0.4 overlay opacity), not a barely-crossed threshold.
- Anchored the reader's place in the story across viewport changes. Progress was derived from the pixel offset into the timeline, and both the viewport height and the timeline's own height change on resize, so the same scroll position mapped to a different era: resizing at "the present moment" (35/62) dropped the reader at 47/62, about 1.6 billion years into the future. `update()` now records the narrative fraction (and, separately, the final-era dwell fraction, which is not narrative time), and a resize handler converts that fraction back into a scroll position before the repaint. Anchoring is skipped when the reader is above or below the timeline so a resize in the hero or conclusion does not drag them into it.
- Deferred the four photographic present-day maps (1.35 MB of the 1.9 MB build) until the present is within 0.08 of the scroll journey. They were fetched at load, so the whole deep past paid for a texture set only one era ever shows; the initial request graph is now HTML, CSS and JS only. The policy lives in `timeline.ts` as a pure `shouldLoadPresentTextures` so it is unit-tested, while `globe.ts` holds the mechanism. Until the maps decode, the present falls back to the same procedural renderer every other era uses — verified on throttled Slow 3G, where the planet reads as a plausible blue marble and then swaps to the photograph, rather than rendering as an unlit black sphere against a blank sampler.
- Gave the timeline a real accessible layer. `[data-era-copy]` carried `aria-atomic` with no live region, so nothing was ever announced; the 62-era chronological list was also `display: none` once JavaScript enhanced the page, which removed the entire content from the accessibility tree. Added a polite, atomic status region that updates only when the active era changes (not per scrolled frame), and made the enhanced fallback visually hidden instead of removed, so the full chronology stays readable by assistive technology. Confirmed in Chrome that the announcer, the `aria-hidden="false"` panel and the globe description all name the same era.
- Recorded a testing gotcha: switching Chrome DevTools emulation between mobile and desktop recreates the document, which resets page state and re-runs `initTimeline`. That looked like a texture regression on resize until a sentinel on `window` showed the document was gone. Genuine resize behaviour has to be checked with a same-class viewport change (e.g. 1920x1080 to 1200x800), where the document survives.
- Rewrote the conclusion and the second paragraph of the present-moment reflection. Both had drifted into abstract nouns — "Endings are transformations", "Earth is temporary, but not insignificant", "we are not the centre of this story… able to remember, reflect, and choose what comes next" — which read as significance asserted rather than earned. The diagnosis that made it fixable: the lines that work on this page name things (oceans, ice, a pixel, a tenth of a second) and the lines that fail name concepts (endings, transformations, significance, story). "Endings are transformations" was also untrue of this particular ending, which the timeline deliberately leaves unresolved at "The models divide here", and the old conclusion simply restated the present-moment panel's argument, so it had nothing new to say and padded.
- The conclusion now runs "None of it was permanent." over "Oceans, ice, forests, extinctions and fire—each built the world that replaced it. Every one of those worlds looked permanent from the inside. Ours does too." That last claim is the one thing sixty-two eras of scrolling actually earns the right to say, and it turns the page back on the reader instead of consoling them. The reflection's second paragraph becomes "In that sliver, the planet grew something that could work out its own age. It has not yet worked out what to do with the answer." — same intent as before, but concrete, and it no longer says we are not the centre while making us the centre.
- Kept the two strongest lines untouched and checked their arithmetic rather than trusting them: 5,000 years of written history is 0.095 s of a 24-hour Earth ("its final tenth of a second") and 0.015 px of the 37,692 px narrative scroll ("less than a pixel of this journey"). Both hold.
- Measured rather than assumed on the tight composition: the reworded reflection wraps to the same line count, so the panel still clears 390x844 by 12px, unchanged and unclipped.
- Made the timeline meter navigable instead of decorative. The rail already rendered a tick at every era's mapped position but was `aria-hidden` ornament, so the only way through 39 viewport-heights was scrolling and the whole page offered two tab stops. The twelve core eras are now real `<button>`s (`MILESTONE_ERA_IDS`, exported from `timeline.ts`), each named for its destination and revealing its short date on hover and focus; the other fifty stay decorative ticks, because sixty-two controls would sit about eight pixels apart on a rail a few hundred pixels tall and put sixty-two tab stops between the wordmark and the conclusion. Measured at 1920x1080: twelve controls, 15x14px hit areas, 23px minimum gap, so no overlap.
- Jumping had to invert the present pause. Era scroll values are story fractions, but raw scroll is stretched by the hold at the present, so a naive `era.scroll * narrativeDistance` lands short of the present and short of everything after it. Added `rawProgressForStory` as the inverse of `progressWithPresentPause`; verified in Chrome that the present, red giant, white dwarf and dinosaur controls each land on their own era, and that the present control lands inside the hold (`is-present-pause`). Jumps are instant rather than smooth — these distances run to tens of thousands of pixels and an animated scroll across that reads as a hang.
- Raised the smallest type tier. Kickers, captions, the meter and the reflection's hold indicator ran 6.7-9.3px; the floor is now about 8-10px (ten declarations, 0.42-0.58rem up to 0.5-0.62rem). Contrast already passed, so this was legibility of size only. The mobile present composition is the tight constraint: the reflection panel now clears the 390x844 viewport by 8px rather than 12px, still unclipped.
- The meter's hover/focus label was being painted over by the Moon. `.earth-figure` is z-index 2 and `.moon` is 4, while `.timeline-meter` was static/auto, so the Moon covered the left ~34px of a label that measurement said was fully on screen. The rail is an overlay control, so it now sits at z-index 5. Worth remembering that "the element's rect is inside the viewport" does not mean it is visible.
- Noted but not fixed: at 390x667 the present reflection panel overflows the viewport by about 77px. Confirmed pre-existing by overriding the type sizes back to main's values in the live page and re-measuring — main clips by 74px there, so the type bump accounts for 3px of it. 390x667 is not a marked viewport, so this is recorded rather than chased.
- Replaced the copy crossfade with a sequential swap. The two era panels were driven by one variable — `from` at `1 - t`, `to` at `t` — so at the midpoint both sat at half opacity and two full paragraphs of body copy plus two display headlines overlapped. Worst at 390px, where the copy column is the whole lower half of the screen. The panels now run on separate `--copy-out` and `--copy-in` variables: the outgoing one finishes fading exactly where the incoming one starts, at mix 0.5, which is also where `active` flips — so the visible swap, the `aria-hidden` swap and the announcer all change together.
- The trade is a narrow band where neither panel is at full strength instead of a wide band where both are half-visible. Tuned it by measuring rather than eyeballing: at the first window the dim band was 22px, so I tightened the swap from mix [0.38, 0.62] to [0.44, 0.56] and it dropped to 11px on mobile and 15px on desktop, against roughly 450px and 610px per era. Doubled-text width is zero at both viewports. A reader would have to stop inside about 2% of a segment to notice, and one pixel of scroll resolves it.
- Locked the contract with a test that sweeps 240 samples across the whole journey (the present pause warps the mapping, so one segment is not enough) and asserts the two layers are never both legible, while also asserting each still reaches full opacity — otherwise hiding both would pass.
- Process note: main had moved on while this was in progress. PRs #15 and #16 were merged mid-task, so the branch I was about to build on was stale; caught it from the file-modification notices and re-branched from the updated origin/main before writing anything. Worth checking `git fetch` before starting each new piece of work rather than assuming the base is current.
- Overhauled the globe's look. Diagnosed from the rendered page rather than the source: the atmosphere shell had no light term at all, so it ringed the night limb exactly as brightly as the day limb — a uniform grey halo, which was the single biggest "cheap CG" tell. The surface lighting compounded it: `daylight = 0.29 + diffuse * 0.86` over a `smoothstep(-0.18, 0.55)` ramp, with the light nearly down the camera axis, lit the whole sphere evenly, so the planet read as a flat painted disc with no terminator.
- Underneath both sat a third problem the screenshots showed but the source hid: measuring long tasks while scrolling the deep past gave 23 tasks with peaks of 843ms. Every era generated three 1024x512 canvases pixel-by-pixel in JavaScript on the main thread. That same cap was also why continents were mush — 1024px of texture stretched over ~1200 device pixels — and an equirectangular map pinches at the poles and seams at the antimeridian.
- Those three shared one cause, so they got one fix: the terrain is now synthesised in the fragment shader from 3D simplex noise sampled at the sphere's own surface position. No map means no resolution ceiling, no seam and no poles, and the per-era CPU work disappears. Re-measured the same scroll sweep afterwards: 0 long tasks, down from 23/843ms. Frame time median 8.3ms, p95 9.3ms at 1049x1049.
- The rewrite collapsed a lot of machinery along with it. Because there is now one continuous elevation field instead of a per-era random one, an era transition is just a blend of scalars and palette colours — so the texture cache, the LRU trim, the retired-texture disposal dance and the from/to sampler pairs all went. `globe.ts` lost ~730 lines net.
- Made continents drift with geological time rather than reseeding per era. The old code gave each of the 62 eras an unrelated random layout, so scrolling reshuffled the planet; offsetting the noise domain by `millionYearsFromNow` means eras a few million years apart barely move while billions of years rearrange the map. The timeline explicitly narrates Pangaea assembling and the continents meeting again, so this serves the copy as well as costing half as much.
- TDD where it was actually testable. `globe.ts` needs WebGL and is fully mocked in the suite, but the era-to-shading mapping is pure, so that came first: `planet-shading.ts` reduces an `EraVisual` to bounded scalars the shader blends, and the tests assert every mode has an explicit entry, every value stays in 0-1, and any pair of eras blends without leaving that range. Made `PlanetMode` runtime-enumerable (`PLANET_MODES`) so exhaustiveness is checkable rather than assumed — a new mode is now a type error and a test failure instead of a planet that quietly renders as bare rock.
- Bugs found by looking at the rendered page, each invisible in the source. The cloud map is an indexed PNG whose deck lives in alpha, so reading `.r` roofed the entire sky over in white blocks. The city-lights map has a noisy near-black floor that turned into blue-grey rectangles once amplified — only visible at all because the new lighting created a night side to show them on; it is now gated and tinted warm. And the present-day Earth was being embossed with relief invented by the noise, which showed up as blocky shading along the terminator at DPR 3; procedural relief now fades out against the photographic maps, which carry their own.
- Two artifacts were my own and worth recording. Offsetting one noise field by a scalar for all three warp components gives three correlated results, which warps the domain along a single axis and marbled the whole globe; each component needs its own offset and frequency. And screen-space derivatives blow up where the surface turns edge-on — one pixel spans an enormous stretch of ground there — which smeared terrain into ribbons around the limb until the bump was faded out by how directly the surface faces the camera.
- Added a cheap level-of-detail fade (`fwidth` of the surface position) so the finest octaves drop out where a pixel covers more ground than they resolve. Procedural noise gets none of the mip-mapping a texture would have had for free, and at DPR 3 the limb was speckling.
- Also relit rather than merely fixed: a tight terminator with a warm scattering band across it, sun glint whose tightness follows a per-era ocean roughness, molten worlds that read as dark crust broken by glowing melt instead of a beige ball (a filmic tone curve desaturates anything lit that brightly from every direction), and a halo whose shape comes from ray geometry — the distance the view ray passes from the planet's centre — so it anchors to the planet's own edge and fades into the night limb.
- Removed the CSS drop-shadow under the canvas. A planet in space has nothing to cast onto, and the offset dark copy was reading as a seam along the lower limb. The blurred CSS atmosphere ring became a wide soft bloom, since the tight ring is now the renderer's job and is light-aware.
- Rebuilt the Moon to agree with the renderer's light direction. It was five identical ringed craters on an evenly lit ball — a golf ball, lit from nowhere in particular. Now soft maria and varied basins over a gradient carrying a real terminator into the lower right, matching the globe.
- Verified at both marked viewports against `pnpm preview`: 1920x1080 and 390x844, `scrollWidth === clientWidth` at both, sampling molten, snowball, Pangaea, present and red-giant eras.
- Measured the globe's actual GPU cost after a challenge from Advay: the machine was fine on frame rate but the GPU sat at 99% and the fans ran. The earlier "8.3ms median frame" claim was worthless — it was a requestAnimationFrame delta, which is clamped to vsync and would have read 8.3ms whether the shader cost 1ms or 8ms. A measurement that cannot fail is not evidence.
- Real method, since EXT_disjoint_timer_query_webgl2 is exposed on macOS but never resolves: scale the canvas past the point where the GPU can hold a refresh interval, then frame delta becomes a genuine signal. 5.76 Mpx -> 33.3ms, 12.96 -> 59.3, 23.04 -> 108.3, so about 4.3ms per megapixel. At the shipped 1.63 Mpx that is ~7ms per frame, ~84% of a 120Hz budget — and on a 60Hz laptop with weaker integrated graphics it is dropped frames, not just fans.
- The cost is almost entirely the noise. The present-day era skips every noise block but keeps all the same lighting, texture sampling, glint, halo and tone mapping, and measured 0.35 ms/Mpx against 3.26 for a procedural era: ~89% of the shader is noise, and the lighting is nearly free.
- Fixed by not evaluating what the era does not use. All 45 noise fields ran on every pixel of every era, including magma networks and crater rims on worlds made of neither, and the entire procedural surface on the one era that replaces it with a photograph. Each block is now behind a uniform test, so every fragment in a draw takes the same branch and there is no divergence cost. Living world 4.3 -> 3.26 ms/Mpx, molten 3.95, present 4.3 -> 0.35. Also capped the drawing buffer (MAXIMUM_DRAWING_PIXELS): the globe is sized as a fraction of the viewport, so a 5K display was asking for this shader over roughly four times the pixels of a laptop, unbounded.
- Tried a 60fps cap and removed it. Advay's objection was right and I had not thought it through: at 60Hz it is a no-op by construction, so it does nothing for most people, and if vsync jitters it skips a frame and judders. It optimised for the one machine in front of me. Worth remembering as a category — a fix that only helps the hardware you happen to be testing on is not a fix.
- Moved the Moon out of CSS and into the renderer. The gradient version could imply a light source but never agree with one, so the Moon and the planet disagreed about where the Sun was. It now shares the scene, the light direction and the same noise library, with a deliberately sharper terminator: no atmosphere means no scattering past its own limb, and that contrast is most of what makes the two read as different kinds of body rather than two shaded circles.
- The canvas had to grow to contain it (the Moon hangs off the wrap's right edge), so it is now inset -16% and the camera pulls back to hold the globe at exactly its previous screen size. That distance is solved from the projected radius of a unit sphere rather than typed in, so it stays correct if the field of view is ever retuned.
- Kept the placement in CSS. `.moon` still exists as a hidden layout anchor and the renderer measures it, so the three media queries that move and resize the Moon keep working and no screen-space constants are duplicated in JavaScript to drift out of step. Had to observe the anchor as well as the canvas: a breakpoint that only moves the Moon would otherwise leave the drawn one at its old position, which is exactly what happened the first time I tested it.
- Two things the close-up caught that page-scale views hid. The lunar surface was scratchy static: ridged noise makes filaments, not craters, and at the Moon's real ~90px size a real crater is one or two pixels, so drawing that field honestly only produces aliasing — it is now basin-scale features only. And the terminator staircased into visible blocks, because screen-space derivatives are computed per 2x2 quad and a hard day/night ramp over a bumped normal quantises to them.
- Fixed a seam I had introduced: wrapping the cloud drift offset in fract() puts a step in the texture coordinate, the sampler reads that as an enormous derivative, drops to the smallest mip for that column, and draws a blurred line straight down the planet. The texture already repeats in hardware; the fract() was not only unnecessary but actively harmful.
- Lunar albedo is about 0.12 — closer to worn asphalt than to snow. It only looks white because it is normally seen against a night sky, and here it sits beside an Earth that reflects three times as much, so the first pass read as a lamp rather than a rock.
- Moved hasVisibleMoon from main.ts into timeline.ts beside moonHeatFor, since the renderer and the globe's accessible description both depend on it and two copies of the same rule can disagree. Tested that the Moon appears only between the impact that formed it and the end, and never blinks out in between.
- The heated Moon was not reading as heated near the Sun's death. The data path was fine — `--moon-heat` reached 1.0 and the uniform was updating — so the bug was purely in how the heat was drawn: the shader tinted the lit surface orange, which pushes the channels past 1.0 where the filmic tone curve desaturates them straight back to cream, and by that point in the timeline the Moon is seen against the red giant's own disc, so a warm surface has nothing to contrast against. It rendered as a pale cream disc on an orange field.
- Same mistake I had already made and fixed on the molten planet, which is the part worth remembering: hot rock is dark rock with light coming *out* of it, not a lit surface painted a warm colour. Under a filmic curve, adding emission below 1.0 keeps its colour; multiplying an already-lit surface above 1.0 loses it. The Moon now darkens to scorched basalt and gains an additive incandescence pooling in the maria (the low basalt plains, where melt would collect) and threading along the basin rims, independent of the daylight term so it carries on to the night side.
- Also worth noting for next time: this is the second time a backtick inside a GLSL comment has silently closed the JavaScript template literal holding the shader. It fails as a parse error a long way from the cause. Comments inside these strings should not quote identifiers.
- Replaced the bundle-only performance check with a two-layer harness. The fast layer still gates deployed bytes and the scroll-mapping hot path; the opt-in Chrome layer now cold-loads the two marking viewports and a constrained laptop, sweeps the full timeline, exercises the deferred present textures and reduced-motion state, and records web vitals, long tasks, frame misses, heap, transfers, browser errors and actual WebGL render activity into Markdown and JSON reports.
- Made the GPU test encode the lesson from PR #20 instead of trusting another vsync-clamped number: it calibrates refresh on a blank page, raises only the diagnostic drawing buffer until the renderer misses vsync, and fits the saturated procedural samples. The first end-to-end run on the M4 measured a 2.34 ms/Mpx slope (R² 0.981), predicted ~7.1 ms at the shipped 1.25 Mpx cap / 85.5% of a 120 Hz interval, and exposed that the renderer keeps issuing ~121 frames/s both below the fold and under reduced motion. Those are warnings for the later optimisation pass, not changes smuggled into this testing task.
- Ran the new deep suite and took the two rendering warnings it had parked. The globe was shading ~121 frames/s below the fold and another ~121 under reduced motion — every one of those frames the full procedural surface, shown to nobody. Both are now gated: an IntersectionObserver pauses the loop when the canvas leaves the viewport, and reduced motion switches from a continuous loop to drawing once when scrolling actually changes the era. Measured after: 0.0 offscreen renders/s on all three profiles, 0.8 under reduced motion (the single repaint on change), and desktop load blocking time fell 15 ms -> 5 ms.
- Put the decision in a tested pure function (`renderModeFor`) rather than scattering conditions through the loop, because globe.ts is WebGL and is mocked out of the DOM tests, so nothing in it was covered. Three states: animate, on-demand, paused.
- The reduced-motion gate did not work on the first attempt and the reason is worth recording. The `change` listener on the globe's MediaQueryList never fired under Chrome's CDP media emulation, while an identically registered listener on a *different* MediaQueryList in the same page did fire — verified by instrumenting the call count, which stayed at 3 across the switch. I could not explain it, so I stopped depending on it: the render loop now re-checks its own licence to run each frame (four property reads) and the observers only need to wake a paused globe, which they demonstrably do. Both paths then measured 0 renders/s.
- The saturation probe pins reduced motion so each frame costs the same, which the new gate would otherwise switch off mid-measurement. It gets an explicit override, keyed on the probe forcing a drawing buffer; plain observation runs get no override and so keep measuring the shipped behaviour rather than a special case.
- Tried trimming the lowest-amplitude noise octaves (fbm5->fbm4 on continents and cloud base, fbm3->fbm2 on grain, moisture and wisps: 29 simplex evaluations per fragment down to 24) and reverted it. The headline duty cycle appeared to fall 87.6% -> 62.5%, but the slope did not move (2.46 -> 2.52 ms/Mpx) — the whole apparent gain was the fit intercept, which swings ~2 ms between runs of identical code. Against the raw saturated points the trim was worth 1-2 ms at 10-15 Mpx, barely above the ~1.3 ms run-to-run spread, for a real cost in coastline and cloud detail. Not a trade worth making silently.
- Which leaves the honest reading of the remaining warning: the fit extrapolates from 5-20 Mpx down to a 1.25 Mpx cap, so the intercept is doing most of the work in that 88% figure and is not trustworthy. The well-determined number is the slope. Differencing procedural against present at 20 Mpx (52.68 vs 12.00) puts the noise itself at ~2.0 ms/Mpx, so roughly 2.5 ms per frame at the shipped cap — worth attacking, but not the 88% emergency the headline reads as. A cubemap bake of the terrain fields is the only change that would cut it substantially, and that is an architecture decision, not a tuning one.
